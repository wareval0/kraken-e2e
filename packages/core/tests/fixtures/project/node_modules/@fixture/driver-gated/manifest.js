import { CONTRACT_VERSION } from '@kraken-e2e/contracts';

export default {
  kind: 'kraken-driver',
  id: 'gated',
  platforms: ['gated'],
  version: '1.0.0',
  contract: CONTRACT_VERSION,
  platformLabel: 'Gated (darwin-only fixture)',
  hostRequirements: { platforms: ['darwin'] },
  disabledFix: 'Run on macOS to use the gated fixture driver.',
};
