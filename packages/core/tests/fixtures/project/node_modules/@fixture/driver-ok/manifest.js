// The /manifest subpath: ZERO heavy imports (ADR-0001 §5.5).
import { CONTRACT_VERSION } from '@kraken-e2e/contracts';

export default {
  kind: 'kraken-driver',
  id: 'fixture',
  platforms: ['fixture'],
  version: '1.0.0',
  contract: CONTRACT_VERSION,
  platformLabel: 'Fixture (string-form test driver)',
};
