import { CONTRACT_VERSION, defineDriver } from '@kraken-e2e/contracts';

export default defineDriver(() => ({
  manifest: {
    id: 'fixture',
    platforms: ['fixture'],
    version: '1.0.0',
    platformLabel: 'Fixture (string-form test driver)',
  },
  start: async () => {},
  createSession: async () => {
    throw new Error('fixture driver has no sessions');
  },
  stop: async () => {},
}));

export const loadedContract = CONTRACT_VERSION;
