# Shared, NON-SECRET run parameters. Copy to .env (gitignored by convention;
# nothing here is sensitive). Loaded into process.env before kraken.config.ts
# is evaluated, so the config can read it.
KAHOOT_HOME_URL=https://kahoot.com/
