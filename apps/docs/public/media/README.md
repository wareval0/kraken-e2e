# Documentation media

Drop screenshots, diagrams and screen recordings referenced by the docs here.
Files in `public/` are served at the site root, so `public/media/hero.mp4`
is referenced as `/media/hero.mp4`.

Currently referenced by placeholders (see the pages that mention them):

- `hero.mp4` (+ `hero-poster.png`) — landing hero recording of the Kahoot run
- `kahoot-run.mp4` — full cross-device run, on the Live Kahoot page
- `kahoot-host.png`, `kahoot-player.png` — the two screens, side by side
- `uniandes.svg`, `sdl.svg` — institutional logos on the landing page
